#include<iostream>
#include<bits/stdc++.h>
using namespace std;
//Macros
#define endl "\n"
#define fn(x,n) for(; x < n; x++)
#define fo(n) for(auto i = 0; i < n; i++)
#define fastio ios_base::sync_with_stdio(false);cin.tie(NULL);

typedef long long ll;
typedef uint64_t ui;

void solve(){
  
}

int main(){
  //fastio

  int cases=1;
  //fastscan(cases);
//   scanf("%d",&cases);
  while(cases--){
    string str = string(6, 'a');
    cout<<str;
  }
  return 0;
}